# =====================================================
# PERMISSIVE .GITIGNORE FOR DEVELOPMENT
# =====================================================
# This allows pushing almost everything for easy development
# Only excludes runtime data and dependencies

# =====================================================
# NODE.JS DEPENDENCIES - ALWAYS EXCLUDE
# =====================================================
node_modules/
*/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# =====================================================
# RUNTIME DATA ONLY - EXCLUDE
# =====================================================
# Log files (runtime logs only, not log configs)
logs/*.log
logs/*/*.log
*.log

# Process files
*.pid
*.seed
*.pid.lock

# =====================================================
# BUILD OUTPUTS - EXCLUDE  
# =====================================================
dist/
build/
*.tgz
*.tar.gz

# =====================================================
# RUNTIME UPLOADS - EXCLUDE
# =====================================================
# Actual uploaded files (not directory structure)
uploads/*
!uploads/.gitkeep

temp-uploads/*
!temp-uploads/.gitkeep

# Runtime backup files (not backup configs)
backups/postgres/*
backups/minio/*
!backups/postgres/.gitkeep
!backups/minio/.gitkeep

# =====================================================
# IDE TEMP FILES - EXCLUDE
# =====================================================
.vscode/settings.json
.idea/workspace.xml

# OS temp files
.DS_Store
Thumbs.db

# =====================================================
# TEMPORARY FILES - EXCLUDE
# =====================================================
tmp/
*.tmp
*.temp
